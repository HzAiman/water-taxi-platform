# Stripe defaults
STRIPE_CURRENCY=myr

# Configure credentials with Firebase Secrets (NOT in this file):
# - STRIPE_SECRET_KEY
# - STRIPE_WEBHOOK_SECRET (optional, needed only if validating webhook signatures)